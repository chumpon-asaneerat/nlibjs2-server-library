const nlib = require('./../../nlib');

const mssqlServer = {
    /** Gets the sql server name. */
    getAppName: function() {
        return 'MS Sql Server';
    }
}
module.exports = exports = mssqlServer;
